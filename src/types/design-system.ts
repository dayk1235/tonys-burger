/* ─── Brand Colors ──────────────────────────── */
export type BrandColor = "primary" | "primaryHover" | "primaryLight" | "secondary" | "secondaryHover" | "secondaryLight";

export type BgColor = "base" | "surface" | "surfaceAlt" | "elevated";

export type TextColor = "primary" | "secondary" | "muted" | "inverse";

export type SemanticColor = "success" | "error" | "warning" | "info";

export type BorderColor = "base" | "light";

/* ─── Spacing ───────────────────────────────── */
export type SpacingToken = "xs" | "sm" | "md" | "lg" | "xl" | "2xl" | "3xl" | "4xl";

/* ─── Radius ────────────────────────────────── */
export type RadiusToken = "sm" | "md" | "lg" | "xl" | "2xl" | "full";

/* ─── Shadows ───────────────────────────────── */
export type ShadowToken = "sm" | "md" | "lg" | "xl" | "glowPrimary" | "glowSecondary";

/* ─── Z-Index ───────────────────────────────── */
export type ZIndexToken = "dropdown" | "sticky" | "overlay" | "modal" | "toast";

/* ─── Animation ─────────────────────────────── */
export type AnimationDuration = "fast" | "normal" | "slow";
export type AnimationEasing = "default" | "in" | "out" | "bounce";

/* ─── Breakpoints ───────────────────────────── */
export type Breakpoint = "sm" | "md" | "lg" | "xl" | "2xl";

/* ─── Container ─────────────────────────────── */
export type ContainerSize = "max" | "narrow" | "wide";
