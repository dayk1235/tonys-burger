/**
 * Types — Barrel Export
 *
 * All TypeScript type definitions are centralized here.
 * Import types from @/types for consistency.
 */

export type {
  BrandColor,
  BgColor,
  TextColor,
  SemanticColor,
  BorderColor,
  SpacingToken,
  RadiusToken,
  ShadowToken,
  ZIndexToken,
  AnimationDuration,
  AnimationEasing,
  Breakpoint,
  ContainerSize,
} from "./design-system";
