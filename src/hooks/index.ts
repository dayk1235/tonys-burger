/**
 * Hooks — Barrel Export
 *
 * Custom React hooks are exported from this directory.
 * Each hook should be in its own file following the
 * useHookName.ts convention.
 *
 * Available hooks:
 * - useGsapAnimation in @/animations (GSAP animation hook)
 *
 * PLACEHOLDER: Add custom hooks here as they are created.
 */
export {};
